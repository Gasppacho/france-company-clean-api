services:
  api:
    build: .
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      PORT: "8000"
      RAPIDAPI_PROXY_SECRET: "${RAPIDAPI_PROXY_SECRET:-}"
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 256M
